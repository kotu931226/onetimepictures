import React from 'react'
import {AppBar, Tabs, Tab} from '@material-ui/core';

function Base() {
    return (
        <React.Fragment>
            <AppBer>
                <span>Onetime Pictures</span>
            </AppBer>
        </React.Fragment>
    );
}

export default Base;
